<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vista de Usuario</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 600px;
      margin: 50px auto;
      padding: 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
    }
    .container {
      background: white;
      border-radius: 15px;
      padding: 40px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }
    h1 {
      color: #667eea;
      text-align: center;
      margin-bottom: 10px;
    }
    .subtitle {
      text-align: center;
      color: #666;
      font-size: 14px;
      margin-bottom: 30px;
    }
    .welcome {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 30px;
    }
    .info-box {
      background: #f5f5f5;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 20px;
    }
    .info-box p {
      margin: 10px 0;
      font-size: 16px;
    }
    .info-box strong {
      color: #667eea;
    }
    .actions {
      display: flex;
      gap: 10px;
      justify-content: center;
    }
    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s;
      text-decoration: none;
      display: inline-block;
      background: #667eea;
      color: white;
    }
    .btn:hover {
      background: #5568d3;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🔐 Vista de Usuario</h1>
    <p class="subtitle">Solo accesible con Token JWT válido</p>

    <div class="welcome">
      <%= message %>
    </div>

    <div class="info-box">
      <p><strong>👤 Usuario:</strong> <%= user.username %></p>
      <p><strong>🆔 ID:</strong> <%= user.id %></p>
      <p><strong>🎭 Rol:</strong> <%= user.admin ? 'Administrador' : 'Usuario' %></p>
      <p><strong>✅ Autenticación:</strong> JWT Verificado</p>
    </div>

    <div class="actions">
      <!-- ⬅️ CAMBIADO: ahora usa onclick en vez de href -->
      <button class="btn" onclick="goBackToProtected()">← Volver a Protected</button>
    </div>
  </div>

  <script>
    // ⬅️ NUEVA FUNCIÓN: Volver a protected usando el token JWT
    function goBackToProtected() {
      const token = localStorage.getItem('accessToken');
      
      if (!token) {
        alert('❌ No se encontró el token JWT. Serás redirigido al login.');
        window.location.href = '/login';
        return;
      }

      // Hacer petición GET a /protected con el token en el header
      fetch('/protected', {
        method: 'GET',
        headers: {
          'Authorization': `Bearer ${token}`
        }
      })
      .then(response => {
        if (response.ok) {
          return response.text();
        } else {
          throw new Error('Token inválido o expirado');
        }
      })
      .then(html => {
        // Reemplazar el contenido de la página con la respuesta
        document.open();
        document.write(html);
        document.close();
      })
      .catch(error => {
        alert('❌ ' + error.message + '. Serás redirigido al login.');
        window.location.href = '/login';
      });
    }
  </script>
</body>
</html>